# Срез К: касса и инкассация под контролем — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** довести деньги от автомата до расчётного счёта так, чтобы на каждом
стыке было видно расхождение: автомат → изъято → пересчитано по купюрам →
сдано в банк, — и чтобы то, что посчитать нельзя, называлось вслух.

**Architecture:** разбивка по номиналам — jsonb на инкассации (один набор на
запись); выписка банка — в существующую `money_flow`, где поле связи с
инкассацией уже спроектировано; сверка по автоматам — по серийнику, механизм уже
работает; реестр пробелов — ВЫЧИСЛЯЕТСЯ на чтении, а не хранится.

**Tech Stack:** Drizzle + Postgres, NestJS, Next.js App Router, `node --test`.

**Методика владельца (правило, которому подчинён весь срез):** считать всё, что
уже можно посчитать; отклонения показывать сразу, в том числе задним числом;
чего посчитать нельзя — не молчать, а держать явный адресный список: что именно,
за какой период, каких данных не хватает; появились данные — расчёт сам.

## Установленные факты (проверены на живом проде 21–22.08.2026)

1. **Инкассация — два шага.** Бот пишет только ФАКТ сбора (кто, когда, какой
   автомат) и прямо говорит: «Деньги передай менеджеру — сумму введут при
   приёме». Сумма появляется в `POST /collections/:id/receive`.
   Значит купюры считает принимающий, а не оператор у автомата.
2. **Разбивки по номиналам нет нигде** — ни в схеме, ни в боте, ни в панели:
   единственное число `collection.amount numeric(15,2)` в сумах.
3. **Все 266 принятых инкассаций кратны 1000** — купюроприёмник берёт от тысячи.
   Номиналы сума: 1 000 · 2 000 · 5 000 · 10 000 · 20 000 · 50 000 · 100 000 ·
   200 000. Медиана инкассации 560 500, максимум 3 145 000.
4. **`money_flow` уже спроектирована под эту задачу и не используется:** колонка
   `collectionId` с комментарием «Связь с инкассацией: наличные из автомата →
   сдача в банк» не пишется НИГДЕ; есть частичный уникальный индекс
   `money_flow_source_ext_key` на `(source, extId)` — «Защита от двойного
   импорта». `FinanceService.createFlow` не принимает ни `source`, ни
   `collectionId` — любая запись через API получает `source='manual'`.
5. **Выписка банка** (`AccReferenceReport*.xlsx`, 2441 строка): колонки Дата
   документа · Счёт · Наименование · Номер документа · Тип документа · Филиал ·
   Оборот Дебет · Оборот кредит · Назначение платежа · **Кассовый символ** · ИНН.
   Взносы наличных помечены символом **`0200`** — 43 взноса на 542 400 000 сум
   с 12.06.2025 по 21.08.2026.
6. **Сверка по автоматам уже возможна:** заказы кофе сопоставляются с карточками
   по СЕРИЙНИКУ (`entity.externalRef`), и все серийники выгрузки сошлись —
   ни одного пропуска. За 01.03–24.06.2026: наличная выручка 144 788 000 против
   инкассированных 141 656 000 = **−2,2%**; 21 автомат из 25 в пределах ±9%.
   Аномалии: Олма Офис **+20%** (+823 000), SKLAD 3F −30%, SKLAD 7C −100%
   (выручка есть, инкассаций нет), Olma Администрация −11%.
   Интервал инкассации 7–15 дней, лаг приёма 2–7 дней.
7. **Наличная выручка ≠ вся выручка:** каналы `cash`/`cash0`/`credit` — наличные,
   `userDefined` и `vip` — безнал (за период 65 905 000 и 4 920 000).
   Сравнивать инкассацию со всей выручкой — ошибка в 34%.
8. **У `collection` нет идемпотентности вовсе** — ни `extId`, ни уникального
   индекса, в отличие от `money_flow` и `task`. Повторный вызов создаёт дубль.
9. **Пробелы, известные на сегодня:** ⚠️ ИСПРАВЛЕНО — см. факт 11 внизу.
   Инкассаций 386, они идут с 26.05.2025 по 30.06.2026; там, где сборы
   регулярны, деньги сходятся до 0,7%. Единственная дыра — окно
   30.07.2025–30.01.2026 на 13 автоматах разом. Снек-продажи считаются
   наличными целиком, потому что в таблице `sale` канала оплаты нет вовсе.
10. Исторические 266 инкассаций перенесены разово из отдельной системы
    (VendCashBot на Railway); скрипта переноса в репозитории нет и не было.

## Ruling'и

- **R-K1. Купюры вводит принимающий, в панели.** Бот сумму не спрашивает вовсе
  (факт 1), и добавлять оператору у автомата восемь полей номиналов значило бы
  просить его считать деньги, которых он не считает. Цена ошибки: если считать
  начнёт оператор — перенесём шаг, данные не пострадают.
- **R-K2. Разбивка — jsonb-колонка на инкассации**, не отдельная таблица: один
  набор номиналов на запись, независимо от него ничего не выбирается. Сумма
  разбивки ОБЯЗАНА совпасть с `amount` — иначе приём отклоняется с указанием
  расхождения. Цена ошибки: агрегаты по номиналам считаются запросом к jsonb,
  что медленнее отдельной таблицы; на сотнях записей несущественно.
- **R-K3. Выписка ложится в `money_flow`** — там уже есть `collectionId` и защита
  от двойного импорта (факт 4). Добавляем колонку `cash_symbol`: прятать признак
  банка в текст назначения значит потом разбирать строку. Цена ошибки: одна
  колонка, которую никто не заполнит, если выписку не грузить.
- **R-K4. Реестр пробелов ВЫЧИСЛЯЕТСЯ, а не хранится.** Хранимый список протух
  бы на следующий день и стал бы третьим механизмом рядом с «не удалось
  проверить» и «требует внимания». Считаем на чтении из тех же данных — тогда
  «появились данные → расчёт сам» выполняется буквально. Цена ошибки: список
  считается на каждый запрос; объёмы сотни строк.
- **R-K5. Сверка по автоматам — по серийнику**, механизм уже работает (факт 6).
  Никакого сопоставления по именам не вводим.
- **R-K6. Сверка с банком — агрегатная по периодам, не построчная.** У 43 взносов
  и 266 инкассаций нет общего ключа операции: банк видит суммарный взнос, система
  — сборы по автоматам. `reconcile.ts` (построчный сравниватель по ключу заказа)
  для этого не годится и переиспользован не будет. Цена ошибки: расхождение
  видно периодом, а не конкретной парой записей — этого достаточно, чтобы
  заметить недостачу.
- **R-K7. Идемпотентность инкассации в этот срез НЕ входит** (факт 8): она
  требует решения, что считать повтором (тот же автомат в ту же минуту?), а
  живых дублей сегодня нет. Записываем как известный пробел, а не чиним походя.

## Global Constraints

- TypeScript strict, без `any`. UI по-русски, код по-английски.
- `pnpm -s build` (11/11), `pnpm -s test` (21/21), `pnpm -s --filter cc lint` — 0.
  Тесты бота гоняются по `dist` → перед ними `pnpm --filter bot build`.
- Часовой пояс Asia/Tashkent; даты — `date`.
- Деньги — сумы, `numeric`; никаких `parseInt`/`Math.trunc` на суммах.
- Миграции применяются автодеплоем БЕЗ отката; только через PR, не прямым пушем.
- Прод-БД на чтение; запись — только через выкаченный код.

## File Structure

- `packages/db/src/schema.ts` + `packages/db/drizzle/0061_cash_reconcile.sql` —
  `collection.denominations` (jsonb), `money_flow.cash_symbol` (text).
- `packages/shared/src/denominations.ts` (+ тесты) — номиналы, сумма набора, сверка.
- `packages/shared/src/bank-statement.ts` (+ тесты) — разбор выписки.
- `apps/core/src/collections/` — приём с купюрами, сверка по автоматам, банк.
- `apps/core/src/gaps/` (новый) — вычисляемый реестр пробелов.
- `apps/cc/` — приём с купюрами, лист «Сверка кассы», лист «Пробелы».

---

### Task 1: Номиналы и сверка набора

**Files:**
- Create: `packages/shared/src/denominations.ts`, `denominations.test.ts`
- Modify: `packages/shared/src/index.ts`

**Interfaces:**
```ts
/** Номиналы сума в обороте автоматов. Все 266 живых инкассаций кратны 1000. */
export const DENOMINATIONS = [200000, 100000, 50000, 20000, 10000, 5000, 2000, 1000] as const;
export type Denomination = (typeof DENOMINATIONS)[number];
/** Сколько купюр каждого номинала. Отсутствующий номинал = ноль купюр. */
export type DenominationCounts = Partial<Record<`${Denomination}`, number>>;
export function denominationsTotal(counts: DenominationCounts): number;
/** Разбор из формы: строки → числа, мусор отбрасывается, отрицательные — ошибка. */
export function parseDenominations(raw: Record<string, unknown>): { counts: DenominationCounts; total: number } | { error: string };
```

- [ ] **Шаг 1: тест сначала** — значения с живых данных:

```ts
it("сумма набора считается", () => {
  // Медианная инкассация 560 500 — не набирается целыми купюрами от 1000,
  // значит в живых данных суммы кратны 1000: проверяем на настоящей.
  assert.equal(denominationsTotal({ "50000": 10, "10000": 6 }), 560000);
  assert.equal(denominationsTotal({}), 0, "пустой набор — ноль, а не ошибка");
});

it("отрицательное количество купюр — ошибка ввода", () => {
  const r = parseDenominations({ "1000": "-5" });
  assert.ok("error" in r);
});

it("дробное количество купюр невозможно", () => {
  const r = parseDenominations({ "1000": "2.5" });
  assert.ok("error" in r, "полкупюры не бывает");
});

it("пустые поля читаются как ноль, а не как ошибка", () => {
  const r = parseDenominations({ "1000": "", "5000": "3" });
  assert.deepEqual(r, { counts: { "5000": 3 }, total: 15000 });
});

it("незнакомый номинал отвергается", () => {
  assert.ok("error" in parseDenominations({ "500": "2" }), "купюр 500 в обороте нет");
});
```

- [ ] **Шаг 2: прогнать — падает. Шаг 3: реализация. Шаг 4: тесты зелёные.**
- [ ] **Шаг 5: экспорт из `index.ts`, коммит** `feat(shared): номиналы и сверка набора купюр`

---

### Task 2: Схема — купюры на инкассации, кассовый символ на движении

**Files:**
- Modify: `packages/db/src/schema.ts`
- Create: `packages/db/drizzle/0061_cash_reconcile.sql`

- [ ] **Шаг 1: проверить дрейф ДО работы** — `drizzle-kit generate` обязан
  сказать «No schema changes». Если нет — доложить контроллеру, не чинить молча.
- [ ] **Шаг 2: колонки.**
  `collection.denominations: jsonb("denominations")` — nullable, комментарий:
  разбивка появляется при приёме и только если её вводили; 266 исторических
  записей её не имеют и это законно.
  `moneyFlow.cashSymbol: text("cash_symbol")` — nullable, комментарий: признак
  банка, взносы наличной выручки помечены `0200`.
- [ ] **Шаг 3: миграция** по образцу `0060_stock_batch.sql`: `ADD COLUMN IF NOT
  EXISTS`, никаких `NOT NULL` без дефолта, ничего не падает на живых данных.
- [ ] **Шаг 4: проверить на чистой временной базе** (0000→0061 и повторный
  прогон, код 0), базу удалить. Прод не трогать.
- [ ] **Шаг 5: снапшот согласован** — `db:generate` снова «No schema changes».
- [ ] **Шаг 6: коммит** `feat(db): купюры инкассации и кассовый символ (0061)`

---

### Task 3: Ядро — приём по купюрам и сверка по автоматам

**Files:**
- Modify: `apps/core/src/collections/collections.service.ts`, `collections.controller.ts`
- Modify: `apps/cc/src/lib/core.ts` (типы и обёртки)

**Interfaces:**
- `POST /collections/:id/receive` принимает необязательные `denominations`;
  если они переданы, их сумма ОБЯЗАНА совпасть с `amount`, иначе отказ с
  указанием обеих цифр.
- `GET /collections/reconcile?from=&to=` — сверка по автоматам:
  наличная выручка (каналы `cash`/`cash0`/`credit`, факт 7) против
  инкассированного, разница, доля, число инкассаций, медианный интервал и лаг.

**R-K11 (контроллер, 22.08). Сверка отдаёт ДВА разреза, а не один.**
Владелец просил дословно: «по автоматам, конкретный период, время МЕЖДУ, с
выручкой и фактически проведённой по системе». Агрегат за окно на этот вопрос
не отвечает — он усредняет и прячет провалы. Поэтому:

- `rows` — по автомату за всё окно (итог, доля, число инкассаций, медианный
  интервал и лаг). Здесь работает проверяемый критерий приёмки ниже.
- `intervals` — ПОСТРОЧНО по каждой инкассации: период от предыдущего сбора
  на ЭТОМ автомате до текущего, ожидаемая наличная выручка за него, фактически
  изъято, разница, длительность в днях.

Цена ошибки, если разрезов не два: провал не виден. Проверено на живых данных —
именно построчный разрез вскрыл дыру в журнале (см. факт 11), а агрегат её
размазывал.

**Готовый и проверенный на проде запрос для `intervals`** — перенеси приём,
не изобретай (он же лежит в основе доказанного факта 11):

```sql
WITH pairs AS (
  SELECT c.id, c.machine_id, c.amount::numeric amt, c.collected_at,
         LAG(c.collected_at) OVER (
           PARTITION BY c.machine_id ORDER BY c.collected_at
         ) prev
  FROM collection c WHERE c.status <> 'cancelled'
)
SELECT p.id, p.machine_id, p.prev, p.collected_at, p.amt,
       COALESCE(SUM(o.amount::numeric), 0) expected
FROM pairs p
LEFT JOIN coffee_order o
  ON o.machine_id = p.machine_id AND o.countable
 AND lower(o.order_resource) IN ('cash','cash0','cash payment','credit')
 AND o.ts > p.prev AND o.ts <= p.collected_at
WHERE p.prev IS NOT NULL
GROUP BY 1,2,3,4,5
```

Три детали куплены чужой болью (донор VendCash, `sales.service.ts:583`) —
не «упрощай» их:
- `WHERE prev IS NOT NULL` — ПЕРВАЯ инкассация каждого автомата выбрасывается:
  у неё нет известного начала периода, и приписывать ей всё «с начала времён»
  нечестно. В витрине это должно быть видно словами, а не молча.
- границы строго `>` и `<=` — иначе заказ в момент сбора попадёт в два периода;
- сопоставление по `machine_id` (серийник), НИКОГДА по адресу или имени.

**Статус вместо голого минуса.** Период длиннее удвоенной медианы по этому
автомату — это `пробел в журнале`, а НЕ недостача. Проверено: при регулярных
сборах сходимость −0,7 %, а все 14 длинных окон дают −92,6 % просто потому,
что инкассации в них не заносили. Показать такое красным минусом значит
обвинить операторов в воровстве там, где виновата дисциплина ввода.

**Критерии приёмки (проверены на проде 22.08.2026):**
- `rows` за 01.03–24.06.2026: **выручка 144 788 000, изъято 141 656 000,
  итог −2,2 %, 25 автоматов**;
- `intervals` целиком: **304 периода до 14 дней дают −0,7 %**, 32 периода
  15–31 день дают −0,7 %, **14 периодов длиннее месяца дают −92,6 %**;
- из этих 14 ровно **13 приходятся на одно окно 30.07.2025 → 30.01.2026**.

- [ ] **Шаг 1: приём с купюрами.** Сверка суммы — в ядре, а не только на форме:
  форма может быть обойдена. Сообщение об отказе называет обе цифры и разницу.
- [ ] **Шаг 2: сверка по автоматам** — по серийнику (R-K5), период задаётся.
  Наличная выручка берётся тем же правилом, что `cashEstimate`
  (`orderIsCash` из `@mydon/shared`), снек — с оговоркой факта 9.
- [ ] **Шаг 3: тесты** по образцу `collections.test.ts`: сумма купюр совпала —
  приём проходит; не совпала — отказ с обеими цифрами; купюр нет — приём
  проходит как раньше (266 исторических записей); сверка на пустом периоде —
  честный ноль, а не ошибка.
- [ ] **Шаг 4: живая проверка на ЧТЕНИЕ** — `GET /collections/reconcile` за
  01.03–24.06.2026 обязан дать итог **−2,2%** и 25 автоматов (факт 6).
  Это лучшая проверка: цифра посчитана независимо и известна заранее.
- [ ] **Шаг 5: прогон и коммит** `feat(core): приём по купюрам и сверка по автоматам`

---

### Task 4: Импорт выписки и сверка с банком

**Files:**
- Create: `packages/shared/src/bank-statement.ts`, `bank-statement.test.ts`
- Modify: `apps/core/src/finance/finance.service.ts`, контроллер
- Modify: `apps/cc/src/lib/core.ts`

**R-K12. Тестовые строки — реальные по ФОРМЕ, обезличенные по реквизитам.**
Проверять разбор надо на том, что ломается: формат даты `дд.мм.гг`, научная
запись числа, символ `0200`, строка-итог без даты, шапка не в нулевой строке.
Всё это в фикстуры входит как есть. А номера счетов и ИНН заменить на заведомо
поддельные — они на разбор не влияют, но реквизиты счёта в репозитории лежать
не должны, даже в приватном. Цена ошибки нулевая: тест проверяет парсер, а не
банк.

**Проверено на самом файле 22.08.2026** (`/Users/js/Downloads/AccReferenceReport20260821223037.xlsx`,
разбор через `parseXlsx` из `packages/shared/dist/xlsx.js`). Бери эти значения как есть:

- `parseXlsx(bytes: Uint8Array, sheetName?)` — **асинхронная**, возвращает
  `{ columns, rows, sheet, sheetNames, ragged }`. Книга: `Лист1 | Лист2 | Лист3`,
  разбирается `Лист1`, `ragged: 0`.
- ⚠️ **`columns` из парсера — МУСОР, шапка лежит в `rows[1]`.** Строка 0 —
  заголовок справки, строка 2 — строка счёта. Настоящие имена колонок:
  `Дата документа`(0) · `Счёт`(1) · `Наименование`(2) · `Номер документа`(3) ·
  `Тип документа`(4) · `Филиал`(5) · `Оборот Дебет`(6) · `Оборот кредит`(7) ·
  `Назначение платежа`(8) · `Кассовый символ`(9) · `ИНН`(10).
  Индексы искать ПО ИМЕНАМ в `rows[1]`, не хардкодить.
- Всего 2445 строк, из них 2440 с датой. Дата — **`дд.мм.гг`, год двузначный**
  (`12.06.25`), а не четырёхзначный.
- Последняя строка — `Итого оборот за период:` без даты; фильтр по формату даты
  её отбрасывает сам, но тест на это обязателен.
- ⚠️ **Числа приходят в научной записи:** в строке итога `1.57328068051E9`.
  `Number(...)` читает её верно, а любая «чистка» вида
  `replace(/[^\d.-]/g, "")` съест `E` и превратит 1,57 млрд в 1,57 — ошибка в
  миллиард раз. Чистить строку регуляркой ЗАПРЕЩЕНО, только `Number()` +
  проверка `Number.isFinite`.
- Кассовые символы: пусто 2016 · `66` 181 · `63` 59 · **`0200` 43** · `11` 41 · `01` 29.
- **Взносы наличных `0200`: 43 штуки на 542 400 000 сум, с 12.06.2025 по 21.08.2026.**
  Это критерий приёмки шага 1: разбор обязан дать ровно эти три числа.
  Пример строки: дата `12.06.25`, наименование «Наличные деньги в оборотной кассе»,
  номер документа `3009296`, оборот кредит `9000000.0`, символ `0200`.

- [ ] **Шаг 1: разбор выписки** — чистая функция поверх `parseXlsx`
  (`packages/shared/src/xlsx.ts`, уже использован для реестра закупок).
  Возвращает строки с датой, суммой по дебету/кредиту, назначением, кассовым
  символом, ИНН, номером документа. Ключ идемпотентности — номер документа плюс
  дата: `extId`. Тесты на реальных строках выписки (см. факт 5), включая формат
  даты `дд.мм.гг` и строку-итог, которую надо отбросить.
- [ ] **Шаг 2: приём пачки** — `POST /finance/bank-statement` c `dryRun`
  и отчётом (создано / повтор / отклонено), как у импорта закупок (срез D).
  Пишем в `money_flow` с `source='bank'`, `extId`, `cashSymbol`.
  **`FinanceService.createFlow` сегодня не принимает ни `source`, ни
  `collectionId`** (факт 4) — расширить, не ломая существующие вызовы.
- [ ] **Шаг 3: сверка периодами** (R-K6): `GET /finance/cash-reconcile?from=&to=`
  — изъято по системе, сдано в банк (символ `0200`), разница, и ОТДЕЛЬНО список
  периодов, где одна из сторон пуста (факт 9).
- [ ] **Шаг 4: тесты** — разбор реальных строк; дедуп по `extId`; отбор по
  символу `0200`; период без инкассаций отдаёт не ноль, а признак «нет данных».
- [ ] **Шаг 5: коммит** `feat(core): импорт выписки и сверка изъято/сдано`

---

### Task 5: Реестр пробелов

**Files:**
- Create: `apps/core/src/gaps/gaps.service.ts`, `gaps.controller.ts`, `gaps.module.ts`
- Modify: `apps/core/src/app.module.ts`

**Interfaces:**
```ts
export interface Gap {
  /** Что именно нельзя посчитать. */
  тема: string;
  /** За какой период — если пробел привязан ко времени. */
  период: { from: string; to: string } | null;
  /** Каких данных не хватает — человеческим языком. */
  чегоНеХватает: string;
  /** Сколько стоит пробел, если это выразимо деньгами или штуками. */
  масштаб: string | null;
  /** Что сделать, чтобы закрылся. */
  чтоСделать: string;
}
```

**Проверено на живом проде 22.08.2026 — это критерии приёмки.** Детектор,
который на сегодняшних данных даёт не это число, СЛОМАН:

| Пробел | Сегодня | Источник истины |
|---|---|---|
| инкассации: тишина | последняя 24.06.2026, **59 дней** | `collection` где `status<>'cancelled'` |
| дыра в журнале инкассаций | **окно 30.07.2025–30.01.2026 на 13 автоматах** | периоды `LAG` длиннее удвоенной медианы |
| заливки без ингредиента | **66 заливок, 62,1 кг** | `coffee_refill.ingredient_id is null` |
| партии без срока годности | **35 из 35** | `stock_batch`, обе даты пусты |
| партии без даты счёта | **2 из 35** | `stock_batch.invoice_date is null` |
| ингредиенты без цены | **0 из 8** | см. ловушку ниже |
| ингредиенты без веса упаковки | **0** (см. ловушку) | `entity.attrs->>'вес упаковки, г'` |
| снек: канала оплаты нет | **968 продаж** считаются наличными целиком | `sale` — колонки канала нет вовсе |
| закупки без даты | **0** | `purchase.dt` |

⚠️ **ЛОВУШКА, НА КОТОРОЙ ДЕТЕКТОР СОВРЁТ. Не читай `coffee_ingredient.purchase_price`
— он пуст у ВСЕХ ВОСЬМИ ингредиентов, хотя цены есть и себестоимость считается.**
Цена живёт в карточке `entity(type='ingredient')`, ключ атрибута — **`цена покупки`**,
единица — **`единица`**. Брать только через `resolveIngredientPrice`
(`packages/shared/src/ingredient-price.ts`), как это делает весь остальной код.
Проверено поимённо: Кофе 260 000/кг · Матча 582 400/кг · MacCoffee 80/г ·
Сахар 13,495/г · Сухое молоко 100 000/кг · Шоколад 182 000/кг · оба чая 147 000/кг.
Детектор на `purchase_price` показал бы **8 несуществующих пробелов** на здоровой
системе — а витрина, которая врёт про пробелы, хуже отсутствующей.

⚠️ **Вес упаковки нужен НЕ ВСЕМ.** Он есть у MacCoffee (20 г) и Матчи (500 г) —
у тех, кого покупают пачками. Остальные шесть идут на вес (`единица: кг`), и вес
упаковки им не нужен. Детектор обязан требовать его только там, где приход
считается штуками, иначе даст **6 ложных пробелов**.

- [ ] **Шаг 1: пробелы считаются из данных** (R-K4), не хранятся. Обязательный
  набор на старте, каждый — из живого факта:
  - инкассации: последняя запись и сколько дней тишины; периоды, где банк
    показывает взносы, а инкассаций нет (факт 9);
  - заливки без ингредиента (двойная позиция бункера);
  - строки реестра закупок без даты прихода;
  - ингредиенты без цены и без веса упаковки;
  - **снек: выручка есть, инкассаций нет вовсе** — проверено 22.08.2026:
    American Hospital · snack 5 258 000, Olma Администрация · snack 11 791 000,
    SKLAD 4S 12 000, итого **17 061 000** без единой инкассации за всю историю.
    Это пробел ввода, а не недостача: показывать отдельным статусом и держать
    ВНЕ итога сходимости (см. фикс задачи 3), иначе он топит сигнал −0,7 %;
  - **сроки годности партий** — **35 из 35** без даты: механизм среза C
    работает, данные не введены. Отчёт по срокам пуст не потому, что всё свежее;
  - **дыра в журнале инкассаций 30.07.2025–30.01.2026** — 13 автоматов,
    ~186 млн ожидаемой наличной выручки против 14,8 млн записанных. Закрывается
    выгрузкой из VendCash (Railway), это действие владельца;
  - **сверка купюр односторонняя**: разбивку вводит принимающий, а отчёта
    САМОГО АВТОМАТА по принятым купюрам в системе нет. Проверено 22.08.2026:
    `raw_report_def` пуст, в коннекторе `ourvend.ts` такого отчёта не заведено.
    Владелец про это знает («когда будет возможность сравнить») — значит пробел
    надо держать в реестре явно, а не молчать о нём: появится отчёт автомата —
    сверка купюр включится сама;
  - снек: канала оплаты нет в данных вовсе, поэтому весь снек считается
    наличными (факт 9) — это пробел модели, а не ввода.
- [ ] **Шаг 2: пустой список — это хорошая новость**, и он должен так и
  выглядеть, а не как ошибка.
- [ ] **Шаг 3: тесты** — на подставных данных: пробел появляется, когда данных
  нет, и ИСЧЕЗАЕТ, когда они появились (это и есть «расчёт сам»).
- [ ] **Шаг 4: коммит** `feat(core): вычисляемый реестр пробелов`

---

### Task 6: Экраны

**Files:**
- Modify: `apps/cc/src/components/collection-receive.tsx` — ввод по номиналам
- Modify: `apps/cc/src/lib/domain-nav.ts`, `apps/cc/src/app/domain/[domain]/page.tsx`
- Create: `apps/cc/src/components/cash-reconcile.tsx`, `gaps-book.tsx`

- [ ] **Шаг 1: приём по купюрам.** Восемь полей номиналов, сумма считается на
  глазах и сверяется с введённой; расхождение показывается ДО отправки.
  Ввести только сумму, без разбивки, по-прежнему можно — 266 исторических
  записей её не имеют.
- [ ] **Шаг 2: лист «Сверка кассы»** по образцу `expiry-book.tsx`:
  показатели (наличная выручка · инкассировано · разница · на руках),
  таблица по автоматам с интервалом и лагом, отклонения больше 10% выделены.
- [ ] **Шаг 3: лист «Пробелы»** — список из Task 5, каждая строка отвечает на
  три вопроса: что нельзя посчитать, почему, что сделать.
- [ ] **Шаг 4: три честных состояния** на обоих листах (данных нет · ядро не
  ответило · фильтр пуст) — урок срезов B, C и D.
- [ ] **Шаг 5: живая проверка**, lint, tsc, коммит.

---

## Что сознательно НЕ входит

- Идемпотентность инкассации (R-K7) — записывается как пробел.
- Сверка с отчётом купюроприёмника автомата: у нас нет ни одного такого отчёта,
  и проектировать под невидимый формат нельзя. Как появится — отдельный срез.
- Канал оплаты у снека: требует источника, которого в выгрузке нет.
- Автоматический сбор выписки из банк-клиента.

## Self-review плана

- Покрыто: купюры (T1–T3, T6), выписка и сверка с банком (T2, T4), сверка по
  автоматам (T3, T6), реестр пробелов (T5, T6).
- Проверяемость: Task 3 шаг 4 сверяется с независимо посчитанной цифрой −2,2%.
- Плейсхолдеров нет: номиналы перечислены, колонки названы, правила отбора
  каналов заданы, ключ идемпотентности выписки определён.
- Типы согласованы: `DenominationCounts` из T1 идёт в T3 и T6; отчёт сверки из
  T3 — в T6; `Gap` из T5 — в T6.

---

## Разведка донора VendCash (21.08.2026)

`~/Projects/_integration-source/VendCashBot` — система, из которой пришли
исторические инкассации. Разработка встала 16.02.2026, но записи в MYDON идут
до 30.06.2026 → **прод на Railway, скорее всего, ещё живёт** и содержит то,
чего у нас нет. Забрать может только владелец (вход в Railway — не моя
операция).

### Что там уже решено и стоит переносить

**Сверка «выручка против инкассации» по автомату за период между сборами**
(`sales.service.ts:583 getReconciliation`) — ровно то, что просил владелец.
Приём: `LAG(collected_at) OVER (PARTITION BY machine_id ORDER BY collected_at)`
даёт начало периода; наличные заказы берутся строго в `(prev, collected_at]`.
Детали, купленные их болью:

- `WHERE prev IS NOT NULL` — **первая** инкассация каждого автомата выбрасывается:
  у неё нет известного начала, и приписывать ей всё с начала времён нечестно.
- Границы строго `>` и `<=` — иначе заказ в момент инкассации попадёт в два периода.
- Сопоставление по **коду автомата**, не по адресу (тот же урок, что я получил
  на своей ошибке со сверкой по полю «адрес»).
- Статусы `matched` / `shortage` / `overage` / `no_sales`; процент отклонения
  считается только при `expected > 0`.
- Порог отклонения — настройка (`reconciliation_tolerance`), не константа;
  выше порога уходит уведомление в Telegram.

**Формула кассы** (`finance.service.ts getBalance`): `остаток = принято от
операторов − сдано в банк`, оба слагаемых в одной транзакции `REPEATABLE READ`,
чтобы снимок был согласованным. У нас роль `bank_deposits` играет `money_flow` —
он богаче (направление, счёт, контрагент), формула та же.

### Чего в VendCash нет

**Купюр нет вообще** — ни поля, ни отчёта (`grep denomination|купюр` пуст).
Задачи 1–3 среза К (номиналы, миграция, приём по купюрам) — новая работа,
переносить нечего.

### R-K8. Время заказа парсится по переменной окружения — это надо закрыть

VendCash напоролся на сдвиг в 5 часов (коммит `9e59220`): ExcelJS отдавал
ташкентское время как UTC, сверка суммировала чужие заказы, чинили миграцией
со сдвигом всех строк на −5 часов.

У нас разбор xlsx безопаснее: `serialToString` (`packages/shared/src/xlsx.ts`)
считает всё в UTC и отдаёт **строку часов, как их видел человек**. Но дальше
`coffee-orders.service.ts:84` делает `new Date(r.ts)` на строке без зоны —
и её смысл определяет `TZ` контейнера. Сейчас `TZ=Asia/Tashkent`, поэтому всё
верно. **Проверено на проде 21.08.2026.**

Правильность держится на переменной окружения, и ни один тест её не стережёт:
уберут `TZ` — все импортированные заказы молча уедут на 5 часов, а сверка по
периодам так же молча станет врать. При этом на **чтении** этот урок уже усвоен
— `coffee-orders.service.ts:172` явно дописывает `+05:00` к голой дате.
Запись обязана делать то же самое.

**Ruling:** в срез К входит явный разбор времени источника с `+05:00` вместо
опоры на `TZ`, плюс тест, который падает при `TZ=UTC`. Цена ошибки — тихий
сдвиг всей сверки; цена фикса — несколько строк.

### Факт 11. Дыра в журнале инкассаций — полгода, все автоматы разом

Сверка методом VendCash прогнана на живых данных прода (386 инкассаций,
26 автоматов, 26.05.2025–30.06.2026):

| Длина периода | Периодов | Ожидалось | Инкассировано | Отклонение |
|---|---|---|---|---|
| до 14 дней | 304 | 205 175 000 | 203 806 000 | **−0,7 %** |
| 15–31 день | 32 | 27 985 000 | 27 797 000 | **−0,7 %** |
| больше месяца | 14 | 201 347 000 | 14 805 000 | −92,6 % |

Там, где инкассации регулярны, деньги сходятся до **0,7 %** — это здоровый
контур, а не недостача. Весь разрыв в 188 млн сидит в 14 длинных окнах, и
13 из них — **одно и то же окно 30.07.2025 → 30.01.2026** на тринадцати разных
автоматах. Одинаковая граница на всех точках означает не пропажу денег, а
пропуск записей: сборы шли, в систему их не вносили.

**Ruling:** длинный период — не «недостача», а признак отсутствующих записей.
Витрина обязана показывать его отдельным статусом (`пробел в журнале`), а не
красным минусом: иначе −92 % прочитается как воровство. Порог — период длиннее
удвоенной медианы по этому автомату.

---

### Task 7: время источника разбирается явно, а не по переменной окружения

**Files:**
- Create: `packages/shared/src/tashkent-time.ts`, `tashkent-time.test.ts`
- Modify: `packages/shared/src/index.ts`
- Modify: `apps/core/src/coffee/coffee-orders.service.ts` (строки ~84, ~89, ~172)

**Зачем задача существует.** Рулинг R-K8 был записан в план и не назначен ни
одной задаче — недосмотр контроллера, закрывается здесь.

Вся сверка этого среза стоит на том, что заказ и инкассация лежат на одной
временной оси. Донор VendCash на этом уже погорел (их коммит `9e59220`):
время из Excel читалось как UTC, сверка суммировала чужие заказы, чинили
миграцией со сдвигом всех строк на −5 часов.

У нас разбор xlsx безопаснее — `serialToString` отдаёт строку часов, как их
видел человек. Но дальше `coffee-orders.service.ts:84` делает `new Date(r.ts)`
на строке БЕЗ зоны, и её смысл задаёт `TZ` процесса. **Проверено на проде
22.08.2026: `TZ=Asia/Tashkent`, `/health` отдаёт `tzOk: true` — сегодня всё
верно.** Но правильность держится на переменной окружения, а не на коде.

Та же дыра на ЧТЕНИИ: помощник `мгновение` (строка ~172) дописывает `+05:00`
только к ГОЛОЙ дате `YYYY-MM-DD`; строка с временем без зоны падает в
`new Date(v)` и так же уезжает.

**Что НЕ делать.** Не трогать `status` в `/health`. Он сейчас `"ok"` даже при
`tzOk: false`, и это стоит починить — но `deploy/auto-deploy.sh:154` гейтит
выкатку именно на `status === "ok"`, так что смена семантики поля меняет
поведение деплоя. Это отдельное решение владельца, а не побочный эффект среза.
Занести в реестр пробелов строкой.

**Interfaces:**
```ts
/**
 * Момент из строки источника. Строка с зоной (`Z`, `+05:00`) — как есть.
 * Без зоны — ташкентские настенные часы, а НЕ часы процесса.
 */
export function tashkentInstant(v: string): Date | null;
/** Голая дата → границы ташкентских суток. */
export function tashkentDayStart(v: string): Date | null;
export function tashkentDayEnd(v: string): Date | null;
```

- [ ] **Шаг 1: тест сначала.** Ключевой тест обязан ловить именно регресс по
  окружению — прогони его при `TZ=UTC` и при `TZ=Asia/Tashkent`, результат
  обязан совпасть:

```ts
it("строка без зоны — ташкентские часы, а не часы процесса", () => {
  // 14:30 в Ташкенте — это 09:30 UTC, независимо от TZ процесса.
  assert.equal(tashkentInstant("2026-06-08 14:30:00")?.toISOString(), "2026-06-08T09:30:00.000Z");
  assert.equal(tashkentInstant("2026-06-08T14:30:00")?.toISOString(), "2026-06-08T09:30:00.000Z");
});

it("строка с зоной не трогается", () => {
  assert.equal(tashkentInstant("2026-06-08T09:30:00Z")?.toISOString(), "2026-06-08T09:30:00.000Z");
  assert.equal(tashkentInstant("2026-06-08T14:30:00+05:00")?.toISOString(), "2026-06-08T09:30:00.000Z");
});

it("голая дата — границы ташкентских суток", () => {
  assert.equal(tashkentDayStart("2026-06-08")?.toISOString(), "2026-06-07T19:00:00.000Z");
  assert.equal(tashkentDayEnd("2026-06-08")?.toISOString(), "2026-06-08T18:59:59.999Z");
});

it("мусор — null, а не Invalid Date", () => {
  assert.equal(tashkentInstant("не дата"), null);
});
```

- [ ] **Шаг 2: прогон под ДВУМЯ поясами** — обязателен, это и есть смысл задачи:
  `TZ=UTC node --test …` и `TZ=Asia/Tashkent node --test …`, оба зелёные.
  Убедись, что до реализации тест под `TZ=UTC` КРАСНЫЙ, если позвать голый
  `new Date` — иначе тест ничего не стережёт.
- [ ] **Шаг 3: применить на записи** — `coffee-orders.service.ts:84` и `:89`.
  Поведение сегодня не меняется (пояс процесса и так ташкентский), меняется
  устойчивость. Существующие данные НЕ трогаем: они импортированы верно.
- [ ] **Шаг 4: применить на чтении** — `мгновение` (~172) переписать на новые
  помощники, сохранив нынешнее поведение для голой даты и для невалидной
  строки (`BadRequestException` с текстом «Дата не разобрана: «…»»).
- [ ] **Шаг 5:** `pnpm -s build`, `pnpm -s test`, `pnpm -s --filter cc lint`.
- [ ] **Шаг 6: коммит** `fix(core): время источника разбирается явно, не по TZ процесса`

