# Консолидация: один источник вместо пяти

Цель владельца: всё сходится в монорепо `mydon`, лишнее убирается.
Правило ТЗ §3.2: **ничего не удаляется до полного бэкапа и тега в git,
решение по каждому активу подписывает владелец.**

## Что уже сведено (сделано)

| Было | Стало |
|---|---|
| 12 паспортов агентов в `mydon-agent-os` | скопированы в `apps/agents/agents/` |
| уведомления сторожей — 3 скрипта, каждый сам в Telegram | события в Core, единые правила (24), журнал |
| command-center без git | ежедневный архив + копия скриптов в `deploy/guards/` |
| экраны панели | новая оболочка `apps/cc` на данных Core |

## Очередь на перенос — по порядку

### 1. AgentOS (контейнер `mydon-mydon-agents`) → `apps/agents`
Новый рантайм уже живёт на сервере (паспорта, политика T0, лимиты, проверка
следов). Осталось: перенести исполнение нав