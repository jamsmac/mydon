# Узлы, инвентаризация, возврат бункеров, товары — что делать владельцу (04.09.2026, статус 05.09)

Код У1–У6 **в `main` и на проде**: #264 `218ee41` (ARMS + У1–У6) · #265 `6d0d584` (нумерация бэкфилла) ·
#266 `40d9837` (сценарии У1–У6 в CI) · #267 `78bc3f7` (экран «Мойка и сушка») · #268 `a9ca9af` (чтения остатков →
леджер) · #269 `5a1cb49` (сверка — та же дверь имён, что леджер). Отметки `[x]` ниже поставлены **по факту на
проде 05.09.2026** (запросы к базе только на чтение); у открытых пунктов — текущее состояние. Один пункт — одно действие.

## 1. Довезти до прода (Mac → GitHub → сервер)

- [x] На Mac в `~/Developer/mydon`: `git status` должен быть чистым на ветке `feat/agentic-os-arms`
      (если git ругается на `.git/index.lock` — удалить файл, это след моих проверок через мост).
      ✓ Ветка смержена (#264), локально ничего не висит: `main` = `origin/main` = `5a1cb49`.
- [x] Локальный гейт перед пушем (зависимости не менялись, `pnpm install` не нужен): `pnpm build && pnpm test`,
      `pnpm --filter @mydon/agents check:passports`. Ожидаемо зелёное: shared 853, db 170, core 1769, agents 473,
      bot 828, cc 386.
      ✓ Текущее зелёное: shared 854 · db 170 · core 1792 · agents 473 · bot 828 · cc 392.
- [x] `git push -u origin feat/agentic-os-arms` → открыть PR в `main` (в main напрямую — нельзя, hookify).
      В описании PR: «ARMS-волна + узлы У1–У6, спека `docs/superpowers/specs/2026-09-04-vendhub-parts-inventory-design.md` §12».
      ✓ PR #264 смержен 04.09 21:56.
- [x] CI: шаг «Migration part_unit backfill (0083 to 0084)» гоняет бэкфилл на настоящем Postgres — если упадёт,
      сначала смотреть его лог, не мержить.
      ✓ Зелёный на #264 и на всех PR после (CI на PR отключён осознанно, гонится `gh workflow run ci.yml --ref <ветка>`).
- [x] Merge → автодеплой применит миграции `0084` (карточка узла + бэкфилл периодов), `0085` (инвентаризация),
      `0086` (мост прайса в реестр). Все три обратимы данными: ничего не удаляется.
      ✓ На проде: таблицы `part_unit`/`part_count_*` есть, бэкфилл 0084 восстановил 150 узлов, `vending_product.entity_id` 52/52.
- [x] После деплоя проверить `docker compose … exec -T mydon-core node tools/smoke-core.mjs` — новый сценарий
      «узел с номером — наклейка, замена по узлу, мойка, инвентаризация, возврат бункера» должен быть `ok`.
      ✓ Сценарий в `tools/smoke-core.mjs` (21 сценарий) гоняется в CI на `postgres:17`; в прод-контейнере smoke
      намеренно не запускается — он пишет и работает только на базе со словом «smoke» в имени.

## 2. Один раз на сервере: паспорта агентов и узлы парка

- [x] Паспорта (из прошлого чек-листа, если ещё не делал):
      `docker compose -f deploy/docker-compose.yml --env-file .env exec -T mydon-core node tools/apply-passport-fields.mjs --dry-run`,
      затем без `--dry-run`.
      ✓ 13/13 агентов с миссией и non-goals в базе.
- [x] **Кофейные автоматы должны иметь карточку `kind=coffee`** (панель → карточка автомата → вид). Автозаведение
      берёт только их.
      ✓ 25 кофейных карточек (19 в работе, 3 на складе, 3 в ремонте) + 5 снек, 1 напитки.
- [x] Автозаведение узлов: `… exec -T mydon-core node tools/provision-parts.mjs --dry-run` — посмотреть план
      (на каждый кофейный автомат: 4 миксера, 1 гриндер, 1 варка, 8 бункеров, 1 фильтр; бункеры получают
      `H-<набор>-<позиция>` по последним заливкам точки, тара — из матрицы 27×8). Затем без `--dry-run`.
      Повторный прогон ничего не дублирует. Узлам, которые бэкфилл 0084 восстановил из журнала БЕЗ номера
      (миксеры, гриндеры, варки), тот же прогон присваивает номер и ставит их в очередь наклеек — строки «№ …».
      Предпросмотр печатает те же номера, что запишет боевой прогон; в панели `/parts` кнопка называет обе
      работы («Завести N узлов и присвоить M номеров»). Состав правится в панели: Система → Настройки → `PARTS_TEMPLATE_COFFEE`.
      ✓ 375 узлов: 225 заведено автоматически + 150 восстановлено бэкфиллом; все 375 стоят в 25 автоматах.
- [x] Проверить в панели `/parts`: узлов ≈ 25 × 15 = 375, все «наклеить номер». Если у кого-то из миксеров уже
      есть свой номер на детали — сотрудник впишет его в очереди (система свой номер отдаст).
      ✓ 375 узлов (25 варок, 25 гриндеров, 100 миксеров, 200 бункеров, 25 фильтров), все 375 с номером и в очереди наклеек.

## 3. Наклейки и права (неделя 1)

- [ ] Роли: у Рустама/Бехруза должны стоять `technician` / `operator` / `storekeeper` — права `parts.number`,
      `parts.move`, `parts.count` идут из ролей (`packages/shared/src/roles.ts`). В боте появятся пункты
      «🔢 Номера узлов», «🚿 Помыл узлы», «🗂 Инвентаризация узлов».
      ⏳ 05.09: у Рустама роли стоят (operator, collector, technician, storekeeper). **Бехруза в реестре людей нет
      вообще** — завести человека (панель → Команда) и поставить роли; без этого его бот не пустит.
- [ ] Напечатать наклейки по списку `/parts` (фильтр «наклеить») — формат `M-001`, `G-001`, `B-001`,
      `H-27-3`, `F-001`.
      ⏳ 05.09: 375/375 ждут наклейки; список для печати готов (панель `/parts` → «наклеить»).
- [ ] Сотрудники проходят очередь: бот «🔢 Номера узлов» → «Наклеил» / «Другой номер» по одному;
      либо панель `/parts/queue`. Пропуск ничего не запоминает — узел вернётся.
      ⏳ 05.09: очередь не начата (0 из 375).
- [x] Каждый понедельник 08:30 агент `vendhub-ops` (навык `parts-audit`) кладёт во Входящие одну задачу:
      сколько ждут наклейки, без номера, неизвестно где, без тары, без фото, что зависло на мойке. Агент
      должен быть `active` (он и так active).
      ✓ `vendhub-ops` active. Первая задача — понедельник **07.09 08:30** (деплой был после прошлого понедельника).

## 4. Замена и мойка — новый порядок работы

- [ ] Замена миксера/бункера/варки: бот «🔧 Замена детали» → автомат → узел → **какой снял (по номеру)** →
      куда увёз (мойка/склад/ремонт) → запасной со склада или «новый» → причина. Записывается одной транзакцией.
      ⏳ 05.09: с деплоя ни одной замены по узлу не записано — проверится на первой.
- [ ] На базе после мойки: «🚿 Помыл узлы» → «Помыл» у узла (уходит на сушку) → позже «На склад».
      Сушку можно отключить: `PARTS_DRYING_STAGE=0` → сразу склад.
      ⏳ 05.09: ни одной мойки по узлу; экран `/parts/washing` пуст по делу.
- [x] Задачи ТО от монитора теперь называют узлы: «Стоят: M-017 (№1), M-018 (№2)…» — проверь на первой задаче.
      ✓ Первая задача 05.09 «Мойка миксера — KIUT M corp»: «Стоят: M-025 (№1), M-026 (№2), M-027 (№3), M-028 (№4)».

## 5. Первая инвентаризация узлов (склад, мойка)

- [ ] Взвесить пустые бункеры без тары — тара вносится на карточке узла (`/parts/<id>` → «Тара, г»); без неё
      возврат не приходуется (очередь «без тары»).
      ⏳ 05.09: тара есть у 103 бункеров из 200 (из матрицы 27×8), **97 без тары** — взвесить.
- [ ] Бот «🗂 Инвентаризация узлов» → склад → по одному: вид → номер с наклейки → серийник → **фото** (или
      причина). «Закончить» даёт сводку. Обрыв связи не страшен — сессия продолжается тем же пунктом.
      ⏳ 05.09: сессий инвентаризации 0.
- [ ] Панель `/parts/count` → сессия → «Применить сессию»: найденные подтверждены, новые заведены,
      не найденные уйдут в «неизвестно где» (найдутся на следующей инвентаризации). Ошибся — «Откатить обратной сессией».
- [ ] Повторить для мойки и сушки. Фото обязательно по умолчанию (`PARTS_COUNT_PHOTO_REQUIRED=1`).

## 6. Возврат бункеров и списание заливок (кофе)

- [x] Ингредиенты кофе должны быть связаны с карточками склада (`coffee_ingredient.entity_id`) — иначе возврат
      запишется, но не оприходуется («у ингредиента нет карточки склада»). Проверить: панель → Кофе → ингредиент →
      карточка реестра.
      ✓ 8/8 ингредиентов с карточкой склада и весом пачки.
- [x] Склад приёма: у склада в реестре поставить признак «приём по умолчанию» (если складов несколько).
      ✓ Склад приёма выбран (сверка `/stock/goods` видит его; «без склада» = 0).
- [ ] Возврат из группы «1. 027. 787» теперь отвечает: «📦 На склад: H-27-1: +377 г Кофе зерновой» или
      «⚠️ Не оприходовано — H-5-3: нет тары». Список непроведённых: `GET /coffee/container-return/unposted`
      (в панели — метка «не оприходовано» у возврата со ссылкой на карточку узла).
      ⏳ 05.09: с 03.08 возвратов не было — новый ответ проверится на первом. 1080 старых возвратов в списке
      «не проведён (до среза)» — это норма, задним числом не приходуются.
- [ ] Списание заливкой включено (`COFFEE_REFILL_CONSUMES=1`), но только по точным данным: число пачек × вес пачки
      (вес пачки — на карточке ингредиента) или веса «до/после». Заливки без данных — `GET /coffee/refill/unconsumed`.
      Не хочешь пока — выключи тумблер в Система → Настройки.
      ⏳ 05.09: тумблер включён (по умолчанию). 26 заливок за 02–04.09 внесены ботом **до деплоя У5** и потому не
      списаны, хотя данные у всех точные (веса «до/после», ингредиент, бункер найден). Решение владельца: списать их
      задним числом (`POST /coffee/refill/<id>/consume` по каждой — 26 движений расхода датой заливки) или оставить
      как «до среза». Новые заливки из бота идут через `POST /coffee/refill` и списываются сразу.

## 7. Товары на перепродажу на складе (У6)

- [x] Панель `/stock/goods` → «Карточки для товаров: план» → «Связать и завести»: у каждого товара прайса
      появится карточка реестра «на перепродажу» (единица шт, цена покупки из прайса).
      ✓ 52/52 позиций прайса с карточкой; «без карточки» в сверке = 0.
- [x] Стартовые остатки из `~/Developer/vending-ops/warehouse.json` (167 единиц на 01.09.2026): скопировать файл на
      сервер (`scp`), положить в контейнер `docker compose -f deploy/docker-compose.yml --env-file .env cp warehouse.json mydon-core:/tmp/warehouse.json`
      и `… exec -T mydon-core node tools/import-warehouse-json.mjs --file=/tmp/warehouse.json --dry-run`,
      затем без `--dry-run`. Это пересчёт с датой 01.09 — двойная запись положит его и в леджер.
      ✓ Пересчёт 01.09 в истории; сверка таблица/леджер по 24 позициям с остатком — `ok`.
- [ ] Неделю работать как обычно (приёмка накладных, заливки, пересчёт из бота — теперь с вкладкой «🍫 Товары»
      в «📦 Приход» и «🧮 Инвентаризация»). Сверка `/stock/goods` должна быть нулевой.
      ⏳ День 1 — 05.09: позиций 52 · без строки 28 · расхождений 0 · без карточки 0 · без склада 0. Неделя — до 12.09.
- [ ] Когда неделя нулевая: Система → Настройки → `VENDING_STOCK_SOURCE=ledger` (катовер). Откат — вернуть `table`.
      Критерий: центральный склад выбран, расхождений 0 и позиций «без строки» с ненулевым леджером нет (панель `/stock/goods`, шапка).
      ⏳ Прод на `table` (по умолчанию). Не переключать раньше 12.09.

## 8. Что осталось на потом (не блокирует)

- Убрать теневую запись `vending_stock` (четыре писателя) и затем таблицу — после недели жизни на `ledger`;
  чтения с таблицы сняты срезом 05.09 (#268), сверка следует той же двери имён, что леджер (#269).
- Дверь имён (`productIndex`): при двух позициях прайса с одним нормализованным именем выбор недетерминирован
  (порядок `select` без `ORDER BY`). Чинить в индексе каталога отдельным срезом, не в сверке.
- ~~Панель: список «мойка/сушка» одним экраном~~ — СДЕЛАНО: `/parts/washing` (ссылка с `/parts`).
  Две очереди работ, сверху лежащие дольше всех, кнопка на строку («Помыл» → сушка или сразу склад,
  «На склад» с сушки); зависшее на мойке дольше 3 суток помечено, как в понедельничной задаче `parts-audit`.
- ~~`pglite`-сценарии в CI~~ — СДЕЛАНО: шаг «Scenarios on real SQL (parts U1-U6)» гоняет их
  на сервисе `postgres:17` в CI, без добавления WASM-пакета в lockfile.
