# Первый вход после ревизии 04.09.2026 — что проверить и поменять

Всё ниже уже **работает с моими значениями по умолчанию**: система не ждёт этих
ответов, чтобы жить. Это список того, где я решил за тебя, чтобы ты прошёлся и
поправил, что не так. Порядок — от «нельзя пропустить» к «когда будет время».
Один пункт — одно действие; сделанное вычёркивай.

## 0. До входа: довезти изменения до сервера (разработчик)

- [ ] Закоммитить рабочее дерево (`git status` — 63 записи, ~130 файлов с новыми каталогами; коммитов я не
      делал: ветка не `main`, сообщение по правилу репо). Каталог `docs/agentic-os-starter/_backup/` — в
      `.gitignore`, после коммита его можно удалить. Перед пушем локально: `pnpm build && pnpm test`,
      `pnpm --filter @mydon/agents check:passports`, `node --test tools/apply-passport-fields.test.mjs`.
      На Mac `git status` мог оставить пустой `.git/index.lock` от моих проверок — если git ругается, удалить его.
- [ ] После деплоя миграция `0083_agent_kb_pages` применится автодеплоем (колонка с default, обратима).
- [ ] Один раз на сервере донести поля паспортов до карточек агентов в базе (seed существующих не трогает):
      `docker compose -f deploy/docker-compose.yml --env-file .env exec -T mydon-core node tools/apply-passport-fields.mjs --dry-run`,
      затем без `--dry-run`. Расхождения «в базе одно, в паспорте другое» скрипт печатает и **не** переписывает.

## 1. Ревизия данных агентов — квиз (149 карточек)

Артефакт «Ревизия MYDON»: https://claude.ai/code/artifact/c45d8513-29d0-4ea0-bd8d-fa844730ab02
По одной карточке на экран: **Подтвердить · Изменить · Удалить · Пропустить**; решённые исчезают.
Группы: 12 агентов (миссия, границы, навыки, расписания, break-glass, KB, веб-источники) и 14 страниц KB.
Что я поставил по умолчанию и что стоит прочитать первым:

- [ ] **Миссии и «чего НЕ делает»** — написал сам для 8 паспортов (`call-analyst`, `chief-of-staff`,
      `globerent-ceo`, `globerent-sales`, `globerent-service`, `knowledge-curator`, `mydon-finance`, `vendhub-ceo`).
      Это текст, который агент читает как границы роли — проверь формулировки.
- [ ] **Break-glass** (навыки, всегда идущие через согласование): `globerent-sales → draft-quote`,
      `globerent-service → draft-service-reply`, `mydon-finance → draft-reminder`. Всё, что пишет наружу или про деньги.
- [ ] **`globerent-sales`**: добавлен навык `intake-classify` (файл был, в паспорте не значился); страницы знаний
      сужены до `heli-models.md`, `pricelist.md`, `lead-criteria.md` — `faq.md` и `clients.md` (дебиторка) убраны
      из контекста квалификации нарочно (least privilege). Вернуть — поле «Страницы знаний (KB)» в карточке.
- [ ] **`market-analyst`**: добавлен навык `read-sources` и 4 веб-источника — xarid.uzex.uz (тендеры),
      cbu.uz (курсы), heli.com.cn (производитель), olx.uz (вторичка, URL с кириллицей — **проверить, что открывается**).
- [ ] **Статусы агентов не менял**: `paused` остались `globerent-ceo`, `globerent-sales`, `globerent-service`,
      `knowledge-curator`, `market-analyst`, `vendhub-ceo`. Включаются кнопкой в панели `/agents/<имя>` — по одному,
      как договаривались («будим постепенно»).

## 2. Первый llm-навык — `globerent-sales / qualify-lead`

Полный сценарий: `docs/AGENTS_ACTIVATION.md` → «Сценарий 1г». Коротко:

- [ ] Тумблеры как для canary 1в (`LLM_ENABLED=1`, `openai-api`, metered) + `AGENTS_TASKS_PAUSED=0`.
- [ ] Включить `globerent-sales` в панели; убедиться, что в карточке есть 3 страницы KB (после п. 0).
- [ ] Поручить одну задачу: «Квалифицируй лид: …» с деталями в описании. Ждать предложение T1 во Входящих;
      в `facts` — `model`, `costUsd`, `kbPages`, `details`.
- [ ] Если задача встала с «Агент ждёт решения владельца» — это `skill_failed` (ответ не по контракту или отказ
      провайдера). Денег второй раз не списывается; читай причину, потом owner retry или переназначение.
- [ ] Второй llm-паспорт предложен через неделю: `globerent-service/triage-service`. Решение — твоё.

## 3. Знания (KB), где стоит `TODO` — агенты честно не знают этих данных

- [ ] `shared/kb/globerent/pricelist.md` — прайс и условия: **заполнить реальными цифрами** (навык `draft-quote`
      и `qualify-lead` цены наизусть не называют — только отсюда).
- [ ] `shared/kb/globerent/faq.md` — каркас под 72 вопроса/ответа; сейчас категории и `TODO`.
- [ ] `shared/kb/globerent/clients.md` — контакты OLMA и др. `TODO`; дебиторка помечена как риск концентрации.
- [ ] `shared/kb/vendhub/machines-map.md` — 31 автомат: локация, тип, пороги — таблица пустая нарочно
      (локации не выдумывал).
- [ ] `shared/kb/vendhub/thresholds.md`, `sop.md` — разделы «Что уточнить (TODO)»: источник телеметрии,
      график/ответственный/частота.

## 4. Роутер репозитория — что я вписал от твоего имени

- [ ] `CLAUDE.md` → «Цели (текущие) — ПОДТВЕРДИТЬ ВЛАДЕЛЬЦУ»: 4 пункта (волна S, волна M, Venture Factory,
      хвосты аудита). Перепиши своими словами или удали лишнее; `/calibrate` в конце сессии будет их обновлять.
- [ ] `memory/decisions.md`, `constraints.md`, `glossary.md` — собраны из репо и спек; проверь, что
      формулировки решений совпадают с тем, как ты их помнишь.
- [ ] `memory/open-questions.md` — 7 открытых вопросов (RBAC enforcement, `PROTOCOL.md`/`MYDON_AGENT_BUILDER.md`
      не найдены, коннектор звонков для `call-analyst`, виджеты календаря/почты…). Ответ = строка в
      `decisions.md`, вопрос удаляется.

## 5. Панель: мелочи, которые заметишь сам

- [ ] В карточке агента появилось поле «Страницы знаний (KB)» — пути `shared/kb/<домен>/<страница>.md`.
- [ ] В карточке задачи блок «Агент ждёт решения владельца» теперь показывает причину `skill_failed` текстом.
- [ ] `.env`: новая необязательная ручка `LLM_SKILL_KB_PAGE_CHARS` (по умолчанию 12 000 символов на страницу KB).

## Напоминание

Раз в неделю (понедельник 09:00 Ташкент) приходит напоминание с этим списком до тех пор, пока ты не скажешь,
что ревизия пройдена — тогда напоминание выключаем. Сам файл лежит в репо (`docs/FIRST_LOGIN_CHECKLIST.md`)
и в проекте MYDON (`claude/FIRST_LOGIN_CHECKLIST.md`).
