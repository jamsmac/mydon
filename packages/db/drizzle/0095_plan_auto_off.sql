-- Почему норматив выключен: системой или человеком (решение владельца
-- 12.09.2026 «мойка миксера появляется по мере выставления номеров»).
--
-- ЗАЧЕМ. `is_active = false` до сих пор значило одно: «владелец решил за этим
-- не следить». Теперь выключать норматив умеет и сверка — когда предмета
-- работы нет в учёте (миксер без наклейки не отличить от соседнего, и «помыл»
-- нечем подписать). Если оставить один флаг на два разных решения, сверка
-- рано или поздно включит обратно то, что владелец выключил руками, — машина
-- молча переиграет человека.
--
-- NULL — выключил человек, трогать нельзя. Заполнено — выключила сверка, она
-- же включит обратно, когда предмет появится.
--
-- ЗАМЕР ПРОДА (12.09.2026, до выката): maintenance_plan — 96 строк, все
-- активные; из них 24 «мойка миксера» при НУЛЕ наклеенных миксеров из ста.
-- Столбец добавляется как NULL, существующие строки не трогаются: всё, что
-- выключено сегодня, остаётся выключенным человеком.
--
-- ОТКАТ: ALTER TABLE "maintenance_plan" DROP COLUMN "auto_off_reason"; —
-- различие исчезнет, и сверка снова не сможет отличить своё выключение от
-- чужого.

ALTER TABLE "maintenance_plan" ADD COLUMN "auto_off_reason" text;
