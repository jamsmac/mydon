-- Кто ФАКТИЧЕСКИ закрыл задачу (аудит видимости 18.08).
--
-- Исполнитель (owner_ref) и закрывший — разные вопросы: задачу сотрудника
-- закрывает и владелец из панели, а лента действий приписывала это
-- сотруднику. NULL у старых строк — честное «не знаем».
ALTER TABLE "task" ADD COLUMN "closed_by" text;
