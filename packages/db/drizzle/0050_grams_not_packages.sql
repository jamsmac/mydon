-- Учёт заливки ведётся в граммах; упаковки считает программа.
--
-- ЗАЧЕМ. Техник сыплет в бункер столько, сколько нужно: иногда половину пачки,
-- иногда полторы. Вопрос «сколько упаковок ушло» заставлял его округлять на
-- глаз, а система потом принимала это округление за факт. Владелец 17.08.2026:
-- «упаковки вводить не обязательно, лучше учёт по граммам, а программа сама
-- считает упаковки».
--
-- ПОЧЕМУ NULL, А НЕ НОЛЬ. Поле было `not null default 1`, поэтому «не
-- спрашивали» было неотличимо от «ровно одна пачка». В базе 1116 строк с
-- единицей, и назвать среди них настоящие единицы уже никто не может. NULL
-- говорит правду: этой цифры нет. Прошлые строки не трогаем — задним числом
-- отличить введённое от подставленного нельзя, а стереть значит потерять и то
-- немногое настоящее, что там есть.
--
-- ОТКУДА БЕРУТСЯ УПАКОВКИ ТЕПЕРЬ. Из веса пачки ингредиента и уже взвешенных
-- граммов. Пока вес пачки не задан, упаковки просто не показываются — это
-- честнее выдуманного числа.
ALTER TABLE "coffee_refill" ALTER COLUMN "package_count" DROP NOT NULL;
--> statement-breakpoint
ALTER TABLE "coffee_refill" ALTER COLUMN "package_count" DROP DEFAULT;
--> statement-breakpoint
ALTER TABLE "coffee_ingredient" ADD COLUMN "package_weight" integer;
