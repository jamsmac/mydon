# deps
node_modules/
.pnpm-store/

# builds
dist/
.next/
out/
build/
*.tsbuildinfo

# turbo
.turbo/

# env & secrets — НИКОГДА не коммитить (только .env.example)
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# os / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# drizzle: миграции КОММИТЯТСЯ (source of truth схемы),
# игнорируем только служебный кэш drizzle-kit
.drizzle/

# Опубликованная оболочка канвы Claude Design (2,3 МБ) — артборды рядом в
# .dc.html читаются и правятся, оболочка пересобирается из них.
design/dashboard-redesign/vendhub-dashboard-redesign.html
# Прототипы, отданные на просмотр: черновики, а не часть панели.
apps/cc/public/prototypes/
