/**
 * Заглушка пакета `server-only` для vitest (алиас в vitest.config.mts).
 *
 * Вне RSC-сборки Next.js специфаер `server-only` не резолвится вовсе — Vite
 * падает на import-analysis ещё до выполнения теста, поэтому `vi.mock` тут
 * бессилен. Настоящий пакет — пустой модуль-маркер, заглушка ему эквивалентна.
 */
export {};
