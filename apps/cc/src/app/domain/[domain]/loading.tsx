/**
 * Состояние загрузки рабочего места.
 *
 * Страница направления ждёт до десятка запросов к ядру, и без этого файла
 * переход между вкладками выглядит как зависший клик: старый экран стоит на
 * месте, пока не придёт новый. Явное «собираем» честнее молчания.
 */
export default function DomainLoading() {
  return (
    <div className="card">
      <div className="hint">Собираем раздел…</div>
    </div>
  );
}
