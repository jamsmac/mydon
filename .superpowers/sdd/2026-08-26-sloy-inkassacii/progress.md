# SDD ledger — plan: docs/superpowers/plans/2026-08-26-sloy-inkassacii.md
Срез «Инкассации: правда о пробеле» (5 задач). Worktree ~/Developer/mydon-inkass,
ветка fix/inkassacii-truth. База была b3b595d (#217) + спека 9f7d61e — смержена
с origin/main (29.08, после П6 #219 и П7 #220), конфликтов не было, полный
прогон после мержа зелёный.

Global Constraints — денежный журнал (386 строк, 264 477 000 сум), операция
НЕ идемпотентна по природе (повторный сдвиг T3 даёт +10 часов). Записей в
прод из задач плана — НИ ОДНОЙ: миграция автодеплоем, бэкфилл ключей и сдвиг
времени — ручные шаги владельца после мержа (раздел «Выкатка»), эта SDD-сессия
их не выполняет.

Task 1: DONE at 9eb9455 — журнальноеДействие() в gaps.service.ts,
ОКНО_СВЕРЕНО/ОКНО_СРАВНЕНИЯ/ДОНОР_СВЕРЕН, детектор 3 перестал советовать
выгрузку из VendCash за проверенное окно. core 1361/1361 (+4), lint/typecheck чисто.
Продолжаю с Task 2 (client_key + миграция + бэкфилл-скрипт — самая большая задача).

Task 2: DONE at 733e75f — client_key колонка+уникальный индекс (миграция
0074, следующий свободный номер после 0073), tashkentMinute/TASHKENT_OFFSET_MS,
script-flags.ts (общий разбор флагов, backfill-product-ids.ts переведён на
него без изменения внешнего поведения — сверено существующим тестом),
backfill-collection-keys.ts (сопоставление донор↔MYDON по коду+моменту+сумме,
неоднозначность печатается не пишется), CollectionsService.create()
идемпотентен по clientKey (onConflictDoNothing + возврат первой строки),
бот генерирует bot:collect:<person>:<machine>:<минута>.
Нашёл и исправил свой баг: счётчик "сопоставлено" был alias кЗаписи.length,
тест "повторный apply: сопоставлено=1, кЗаписи=0" его поймал.
migrations-chain.test.ts НЕ заводил — дублирует уже существующий (после
мержа П6/П7) migrations.test.ts, план писался до его появления.
Проверено на живом Postgres: полная цепочка миграций, NULL-уникальность,
onConflictDoNothing. shared 814/814, db 136/136, core 1366/1366, bot 780/780.
Продолжаю с Task 3 (сдвиг +5 часов — самая деликатная часть среза).

Task 3: DONE at 5bb3bd9 — fix-collection-time.ts (4 заставы: происхождение,
событие, аудит, распределение часов >19; одна транзакция, before/after
построчно), ACTION_LABELS экспортирован из audit/page.tsx.
Отклонение: тест-стенд из плана не различал таблицы event/auditLog/collection
при подсчёте застав (опция "события" протекала бы в застава-1 count) —
переписал на дифференциацию по ссылке на таблицу.
Проверено на живом Postgres реальным сдвигом (не только стендом): +5ч точно,
суммы не изменились, audit_log/event корректны, повторный прогон заблокирован.
db 151/151, cc 256/256.
Продолжаю с Task 4 (рунбуки, кода нет) и Task 5 (smoke + CI).

Task 4: DONE at 439ac80 — рунбуки (архив донора VendCash, разовые шаги),
DATA_SOURCES.md дополнен фактами архива. Проверено: ни строки подключения,
пароля или боевого IP в новом тексте.

Task 5: DONE at de95663 — сторож «писатель ровно один», smoke-collections.mjs,
CI wiring, аддендум спеки (§12, 6 отклонений). Смоук нашёл и закрыл ТРИ
реальных бага (детали в аддендуме и коммите): camelCase/snake_case рассинхрон
в DonorCollectionRow (postgres.js не транслирует имена колонок — юнит-тест на
заглушке этого никогда бы не поймал), process.exitCode вместо process.exit в
обоих скриптах (незакрытое соединение вешало бы прод-прогон после ошибки),
и собственная ошибка обёртки обязанОтказать(прогонОжидаяКод(...)) — двойное
отрицание провалило бы проверку правильного кода отказа.
Прогнан дважды на реальном scratch-Postgres, полная уборка подтверждена.

Все 5 задач среза «Инкассации» ЗАКРЫТЫ. Продолжаю: мерж с origin/main,
полный прогон, adversarial-ревью (особо внимательно — денежный журнал),
PR, мерж.
