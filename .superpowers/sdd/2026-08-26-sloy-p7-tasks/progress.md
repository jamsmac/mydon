# SDD ledger — plan: docs/superpowers/plans/2026-08-26-sloy-p7-tasks.md (ещё не написан)
Срез П7 «Задачи: подтверждение, уведомления, мост событие→задача» (задача #19). Worktree ~/Developer/mydon-p7, ветка feat/p7-tasks = main b3b595d. Инвентаризация: inventory.md — модуль task в монорепо ЕСТЬ и сильнее донора (20 колонок, client_key UNIQUE, 17 роутов, бот «Мои задачи», панель, агенты); проблема — никто не кормит (4 задачи за всю историю, 0 комментариев); донор tasks 0 строк; мост maintenance → задачи молчит (77 нормативов, 19 просрочено, задач 0 за 20 дней) — причина в «Гигиене» (#16).
Рулинги (контроллер): R-P7-1 переносим из донора только две идеи — статус «подтверждено менеджером» и веер уведомлений по правам; модуль остаётся ОДИН для людей и агентов. R-P7-2 ключ дедупа моста «событие → задача» = `<источник>:<сущность>:<дата Ташкента>` (совместимо с task_source_key), одна задача на автомат на день, не на позицию; потолок автозадач на прогон (настройка, 20). R-P7-3 права: tasks.assign/tasks.confirm = роли owner|manager из person.roles с фолбэком легаси role='владелец' (как в П5b); всем — tasks.own. R-P7-4 источники моста в этом срезе: machine.low_stock, vending.refill_detected без сопоставления (unmatched), застой/офлайн сбора (ourvend.sync_stale / sync_failed_streak), vending.shrinkage_alert; maintenance.due — после починки моста в «Гигиене» (ссылка). R-P7-5 task.overdue — эмитент заводим (крон утром), мёртвое правило оживает; подтверждение менеджером — статус confirmed + событие task.confirmed в ленту «Действия».
Спека: 112612d (8 задач; отклонения приняты: confirmed_at/confirmed_by вместо enum (10 мест ne(status,'done')); мост — крон Core 06:15 поверх EventsService.list (правила — опрос, не подписка); автозадачи рождаются свободными (VENDING_ROUTE_ORDER не маршрутный лист); push-hours только для T2/T3; docs/AGENTS.md нет → AGENTS_ACTIVATION.md; ожидание 0–3 автозадачи в первое утро) → план

---
## Продолжение 29.08 (после мержа П6)

Ледгер не вёлся предыдущим прогоном, но git log показал Task 1–5 реально
закоммичены (c6d0666, f9eff32, 66eed88, 9ff2f11, 9729a31) — все пять
заголовков коммитов дословно совпадают с заголовками задач плана.
В рабочем дереве найден незакоммиченный черновик Task 6 (task-confirm.ts
+ test + правки core-client.ts/index.ts/staff.ts), файлы на 2.5 мин
младше коммита Task 5 — тот же обрыв недельным лимитом, что был у Task 7
в П6. Проверил построчно против спеки (§«Что обязана делать реализация»):
исключение закрывшего, claim-до-отправки, порядок comment→quality,
тихие часы — всё верно, 14/14 тестов зелёные. Один реальный пробел:
правило `tasks.no_confirmers` не зарегистрировано в `RULES` (эмитируется,
но осталось бы без правила — урок П5b N5). Дописал правило + тест,
core 1310/1310, bot 741/741, lint чист.
Task 6: DONE at 3c1ae20 (довершён восстановленный черновик + фикс rules.ts)

Ветка НЕ смержена с origin/main (main ушёл вперёд на П6 — 1b40a8e).
Слияние — по чек-листу «Выкатка», не раньше готовности всех 8 задач.
Продолжаю с Task 7.

Task 7: DONE at d769d46 — bot 746/746, cc 240/240 (5 новых в awaiting-block.test.tsx),
core 1310/1310, typecheck 17/17, lint 17/17, live scratch-Postgres:
tools/smoke-panel.mjs 29/29 страниц (оба шага /tasks зелёные).
Продолжаю с Task 8 (последняя).

## Мерж origin/main (29.08, после мержа П6 #219)

`git merge origin/main` — 6 конфликтов: apps/bot/src/menu.ts (два независимых
пункта меню — оставлены оба), apps/core/src/registry/actions.service.test.ts
(две независимые группы тестов — объединены), apps/core/src/system/config-spec.ts
(два независимых блока тумблеров — оставлены оба), packages/db/src/migrations.test.ts
(add/add — обе ветки завели один и тот же файл-сторож независимо; взял версию
origin/main — она строже: проверяет обе стороны файл↔журнал и padStart номера).

**Коллизия номера миграции:** обе ветки заняли 0072 (`0072_product_fiscal_and_storno`
у П6, уже смерженного в origin/main, и `0072_task_confirmation` у П7). Как и
предупреждал план (Выкатка п.2): переименовал файл П7 в `0073_task_confirmation.sql`.
Снапшот и запись журнала НЕ писал руками — прогнал `pnpm --filter @mydon/db
db:generate` от P6-baseline (временно откатив journal до idx 72), сверил
автосгенерированный SQL с моим рукописным: набор колонок/индексов совпал
буквально, но автогенератор потерял BACKFILL UPDATE (`assign_notified_at =
created_at` для старых назначенных задач) и `IF NOT EXISTS` — то, что
`docs/DEPLOY.md` прямо требует проверять после выката. Оставил свой SQL,
взял только корректный снапшот/journal-запись автогенератора (`prevId` верно
цепляется к id снапшота П6). Проверено дважды на живом Postgres: полная
цепочка 0–73 с нуля, и отдельно — бэкфилл на БД с уже существующей
`owner_ref`-задачей (before: assign_notified_at=NULL, after: = created_at;
свободная задача осталась NULL; 0 незабэкфилленных строк).

Полный прогон после мержа: lint 17/17, typecheck 17/17, build 21/21, test
21/21 (shared 811, db 118, connectors 80, assistant 46, documents 5, agents 274,
bot 777, core 1356, cc 253 — 0 fail). Живой scratch-Postgres: smoke-core.mjs
46 чтений/19 записей/18 сценариев, smoke-panel.mjs 29/29 страниц — обе ветки
(сторно П6 + мост/приёмка П7) работают вместе без конфликтов на уровне SQL.
